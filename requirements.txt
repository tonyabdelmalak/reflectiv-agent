flask
# Use a recent version of the OpenAI Python library. Version 1.0.0 and above
# introduces the new client API (`OpenAI` class) and supports project keys
# (sk‑proj). This server has been updated to use the new API.
openai>=1.0.0
twilio
flask-cors